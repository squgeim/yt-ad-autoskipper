export const SKIP_AD_BTN_CLASSES = [
  "videoAdUiSkipButton", // Old close ad button
  "ytp-ad-skip-button ytp-button", // New close ad button
];

export const BANNER_AD_BTN_CLASSES = [
  "ytp-ad-overlay-close-button", // Close overlay button
];

// Milliseconds after with Skip Ad button is activated by default.
export const AD_PLAYBACK_OFFSET = 5000;
